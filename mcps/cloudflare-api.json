{
  "mcpServers": {
    "cloudflare-api": {
      "url": "https://mcp.cloudflare.com/sse",
      "type": "sse",
      "headers": {
        "Authorization": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    }
  }
}
