# === justfile ===
# Purpose:  Lands at repository root. Canonical recipe vocabulary (install/format/lint/test/check/update/dev/build/start/release). CI workflows call ONLY `just <recipe>`. See references/ci-contract.md for the variant matrix.
# Tokens:   none
# Override: Fill in recipe bodies per stack. Add repository-specific recipes below the canon set. Do not rename canon recipes — workflows depend on the vocabulary.
# === end ===

default:
    @just --list

# Provision local dev environment. Idempotent.
install:

# Auto-fix formatting + safe lint fixes. Writes files.
format:

# Static analysis. No writes.
lint:

# Run test suite.
test:

# Scan deps for known vulns. Implementation per stack: `uv audit`, `cargo audit`, `pnpm audit`.
audit:

# CI gate. Compose: lint + test + audit + build (where applicable).
check:

# Bump deps to latest compatible. Touches lockfiles.
update:

# Local dev server with hot-reload (next-site only).
dev:

# Production artifact.
build:

# Production-mode local run (build + serve).
start:

# Cut a release tag. Usage: just release v1.2.3
release version:
